{
  "minimumVersion": "3.8.50",
  "requireImmutableRelease": false,
  "requireAnnotatedTag": true,
  "requireExactCommitPin": true,
  "requiredCapabilities": [
    "antigravity-gemini-3.7"
  ],
  "plannedOptionalCapabilities": [
    "cloudflare-images",
    "aihorde-images"
  ],
  "productionDeploy": "manual-only",
  "notes": "Advance baseline only to a published upstream tag resolved to an exact commit SHA. GitHub release immutability is not required because upstream v3.8.50 is not marked immutable; the baseline commit pin is authoritative. Optional image adapters remain independent overlay work."
}
